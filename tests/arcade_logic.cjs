const fs=require('node:fs'),vm=require('node:vm'),assert=require('node:assert/strict');const elements=new Map();const ctx=new Proxy({},{get:()=>()=>{},set:()=>true});function el(s){if(!elements.has(s))elements.set(s,{textContent:'',open:true,addEventListener(){},querySelectorAll(){return[]},getContext(){return ctx},showModal(){},focus(){}});return elements.get(s)}const scope={document:{querySelector:el,addEventListener(){}},window:{addEventListener(){}},performance:{now:()=>0},requestAnimationFrame:()=>1,cancelAnimationFrame(){},Math};let src=fs.readFileSync('assets/js/snake.js','utf8');const pos=src.lastIndexOf('})();');src=src.slice(0,pos)+`globalThis.test={reset,update,hit,setPause,state:()=>({stage,lives,running,paused,enemies:enemies.length}),go:()=>running=true,clear:()=>enemies=[],vulnerable:()=>invincible=0};`+src.slice(pos);vm.runInNewContext(src,scope);const t=scope.test;t.reset();t.go();assert.equal(t.state().enemies,18);t.clear();t.update(.01);t.update(2);assert.equal(t.state().stage,1);t.clear();t.update(.01);t.update(2);assert.equal(t.state().stage,2);t.clear();t.update(.01);assert.equal(t.state().running,false);assert.match(el('#snake-status').textContent,/Misión completa/);t.reset();t.go();t.hit();t.hit();assert.equal(t.state().lives,2);t.vulnerable();t.hit();t.vulnerable();t.hit();assert.equal(t.state().running,false);t.reset();t.go();t.setPause(true);assert(t.state().paused);console.log('PASS: all three stages, win, damage immunity, defeat and pause.');
